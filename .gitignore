# Raw archival scans (huge, not used by the site — site only uses jpg/png/etc.)
*.tif
*.tiff

# Node / Vite
node_modules/
dist/
.vite/

# Config local à cette machine/session (chemins absolus, historique d'outils) —
# jamais à publier.
.claude/
